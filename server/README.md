# Backend (Flask + MySQL)
Quick start:
1) python -m venv .venv && . .venv/Scripts/activate (Windows) or source .venv/bin/activate
2) pip install -r requirements.txt
3) Copy .env.example to .env and fill DB/SMTP
4) Run schema.sql on MySQL
5) python app.py
